# Module 1.17 Flow Chart

1. Understand Asymmetric Key Pair Cryptography
2. Generate SSH Key Pairs (`ssh-keygen`)
3. Master Key Transfer Mechanisms (`ssh-copy-id`, `authorized_keys`)
4. Audit OpenSSH Server Hardening Guidelines (`/etc/ssh/sshd_config`)
