# Module 1.2 Flow Chart

1. Understand Filesystem Hierarchy Standard (FHS)
2. Explore Critical Paths (`/etc`, `/var/log`, `/proc`)
3. Identify Security Relevance of Temporary Paths (`/tmp`)
4. Practice Navigation & Inspection (`ls -la /`, `ls -l /var/log`)
