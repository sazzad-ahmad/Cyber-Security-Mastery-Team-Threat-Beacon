# Module 1.13 Flow Chart

1. Learn Regular Expressions & `grep` Filtering
2. Master Field Extraction using `awk` and `cut`
3. Apply Stream Editing using `sed`
4. Practice Pipeline Aggregation (`grep -v`, `sort | uniq -c`)
