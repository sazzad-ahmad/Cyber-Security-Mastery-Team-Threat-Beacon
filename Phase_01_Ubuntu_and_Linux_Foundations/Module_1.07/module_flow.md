# Module 1.7 Flow Chart

1. Learn User Identifier Hierarchies (UID 0 vs System vs Regular)
2. Inspect Authentication Core Files (`/etc/passwd`, `/etc/shadow`)
3. Master User Management Commands (`id`, `useradd`, `usermod`)
4. Execute Inspection Commands (`id`, `grep "^$USER" /etc/passwd`)
